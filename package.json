{
  "name": "repo-sprint-digest",
  "version": "1.0.0",
  "description": "GitHub Action that monitors repository changes and provides summaries of merged PRs grouped by date",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "start": "node src/index.js",
    "build": "ncc build src/index.js -o dist",
    "prepare": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@octokit/rest": "^19.0.7",
    "dotenv": "^16.0.3",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}
